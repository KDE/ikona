ikona_sources = ['iconsetter.cpp', 'main.cpp']

ikona = executable('ikona',
                   ikona_sources,
                   processed_files,
                   include_directories: inc,
                   dependencies: [qt5_dep, kde_deps],
                   install: true)
